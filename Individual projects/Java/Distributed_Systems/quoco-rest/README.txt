I was unable to get the docker images working correctly however i successfully implemented the assignment as far as part 4.
Run the services auldfellas,dodgydrivers,girlpower first using mvn sprint-boot:run in the folders of the same names.
Then run the broker using mvn sprint-boot:run in the broker folder.
Finally run the client using mvn sprint-boot:run in the client folder and you should see each customer's quotations being printed to the console.